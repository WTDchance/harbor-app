// components/Sidebar.tsx
//
// Wave 21: dead code. The active dashboard sidebar lives in
// app/dashboard/layout.tsx (rebuilt for Cognito). This file used to
// render a parallel sidebar with Supabase auth — no remaining imports
// reference it. Kept as an empty export so any stale build manifest
// doesn't 404.

export function Sidebar() {
  return null
}
